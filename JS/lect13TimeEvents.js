//TIMING EVENTS IN JAVASCRIPT
// window allow the execution of code at specific time Interval, these time interval is known as TIME EVENTS. 

// the two key method we use in JS are :
//SET TIMEOUT (setTimeout(functions, miliseconds))
//executes a function after waiting a specified number of miliseconds
//SETINTERVAL (setInterval(functions, miliseconds))
//same as  setTimeout(), but repeats the execution of the function continously.

//1. setTimeout()

//2. clearTimeout()

//3. setInterval()

///4. clearInterval()